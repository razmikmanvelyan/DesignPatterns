#include "VulkanRenderer.hpp"

void Bridge::VulkanRenderer::renderCircle(int x, int y, int radius) {
    std::cout << "Vulkan Renderer: circle at " << x << ':' << y << " radius " << radius << std::endl;
}