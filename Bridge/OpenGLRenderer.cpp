#include "OpenGLRenderer.hpp"

void Bridge::OpenGLRenderer::renderCircle(int x, int y, int radius) {
    std::cout << "OpenGL Renderer: circle at " << x << ':' << y << " radius " << radius << std::endl;
}